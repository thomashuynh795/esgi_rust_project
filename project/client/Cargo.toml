[package]
name = "client"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "worker"
path = "src/main.rs"

[dependencies]
shared = { path = "../shared" }
grid = { path = "../grid" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
